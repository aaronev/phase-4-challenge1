<!DOCTYPE html>
<h6><%= review.timestamp %></h6>
<div class="container">
  <p><%= review.review%></p>
  <% if (userSess) { %>
    <% if (userSess.id === review.user_id) { %>
      <img class="delete trash" onclick="confirmDelete(<%=review.id%>)" src="/img/trash.png"/>
    <% } %>
  <% } %>
</div>
